function appearTest() {
    document.getElementById('disappear').innerHTML = "Je naam is " + promptTest;
    document.getElementById('disappear').style.visibility='visible';
}
function returnTest(number) {
    return(number)
}
function testForFunction(numberExtra) {
    var a = returnTest(numberExtra)
    return(a)
}